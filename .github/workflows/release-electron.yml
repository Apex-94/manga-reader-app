name: Release Electron

on:
  push:
    tags:
      - "v*"

jobs:
  release-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: |
            frontend/package-lock.json
            electron/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Build bundled backend exe
        shell: powershell
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt pyinstaller
          Set-Location backend
          pyinstaller pyinstaller.spec --clean

      - name: Install electron dependencies
        working-directory: electron
        run: npm ci

      - name: Build Windows installer
        working-directory: electron
        run: npm run build:win

      - name: Collect release assets
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path release-assets | Out-Null
          Copy-Item electron/release-electron/*.exe release-assets/ -ErrorAction SilentlyContinue
          Copy-Item electron/release-electron/*.blockmap release-assets/ -ErrorAction SilentlyContinue

      - name: Publish GitHub release assets
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release-assets/*.exe
            release-assets/*.blockmap
          prerelease: ${{ contains(github.ref_name, 'beta') }}
