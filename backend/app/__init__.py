"""
Backend application package.

This file marks the `app` directory as a Python package. It does not
contain any functional code but allows modules within this directory to
be imported using the `app.*` namespace.
"""
